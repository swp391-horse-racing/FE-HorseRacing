#!/bin/sh
# Chặn Co-authored-by Cursor / cursoragent — mọi thành viên dùng core.hooksPath=.githooks
msg_file="$1"
[ -f "$msg_file" ] || exit 0

tmp="${msg_file}.tmp"
grep -viE '^co-authored-by:.*(cursor|cursoragent|cursor agent)' "$msg_file" > "$tmp" || true
mv "$tmp" "$msg_file"
exit 0
